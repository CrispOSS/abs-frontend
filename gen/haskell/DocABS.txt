The Language ABS
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of ABS==
===Identifiers===
Identifiers //Ident// are unquoted strings beginning with a letter,
followed by any combination of letters, digits, and the characters ``_ '``
reserved words excluded.


===Literals===
String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.


Integer literals //Integer// are nonempty sequences of digits.




TypeIdent literals are recognized by the regular expression
`````upper (letter | digit | '_' | ''')*`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in ABS are the following:
  | ``assert`` | ``await`` | ``builtin`` | ``case``
  | ``class`` | ``data`` | ``def`` | ``else``
  | ``export`` | ``extends`` | ``fimport`` | ``from``
  | ``get`` | ``if`` | ``implements`` | ``import``
  | ``in`` | ``interface`` | ``let`` | ``local``
  | ``module`` | ``new`` | ``null`` | ``return``
  | ``skip`` | ``spawns`` | ``suspend`` | ``then``
  | ``this`` | ``thisDC`` | ``type`` | ``while``

The symbols used in ABS are the following:
  | , | ; | * | .
  | _ | < | > | =
  | ( | ) | | | {
  | } | ? | & | ||
  | && | == | != | <=
  | >= | + | - | /
  | % | ~ | [ | ]
  | => | ! |  | 

===Comments===
Single-line comments begin with //.Multiple-line comments are  enclosed with /* and */.

==The syntactic structure of ABS==
Non-terminals are enclosed between < and >. 
The symbols -> (production),  **|**  (union) 
and **eps** (empty rule) belong to the BNF notation. 
All other symbols are terminals.

  | //AnyIdent// | -> | //Ident// 
  |  |  **|**  | //TypeIdent// 
  | //[AnyIdent]// | -> | //AnyIdent// 
  |  |  **|**  | //AnyIdent// ``,`` //[AnyIdent]// 
  | //Program// | -> | //[Module]// 
  | //[Module]// | -> | **eps**
  |  |  **|**  | //Module// //[Module]// 
  | //Module// | -> | ``module`` //QualType// ``;`` //[Export]// //[Import]// //[Decl]// //MaybeBlock// 
  | //Export// | -> | ``export`` //[AnyIdent]// 
  |  |  **|**  | ``export`` //[AnyIdent]// ``from`` //QualType// 
  |  |  **|**  | ``export`` ``*`` 
  |  |  **|**  | ``export`` ``*`` ``from`` //QualType// 
  | //[Export]// | -> | **eps**
  |  |  **|**  | //Export// ``;`` //[Export]// 
  | //Import// | -> | //ImportType// //QualType// ``.`` //AnyIdent// 
  |  |  **|**  | //ImportType// //[AnyIdent]// ``from`` //QualType// 
  |  |  **|**  | //ImportType// ``*`` ``from`` //QualType// 
  | //[Import]// | -> | **eps**
  |  |  **|**  | //Import// ``;`` //[Import]// 
  | //ImportType// | -> | ``fimport`` 
  |  |  **|**  | ``import`` 
  | //Type// | -> | ``_`` 
  |  |  **|**  | //QualType// 
  |  |  **|**  | //QualType// ``<`` //[Type]// ``>`` 
  | //[Type]// | -> | //Type// 
  |  |  **|**  | //Type// ``,`` //[Type]// 
  | //[QualType]// | -> | //QualType// 
  |  |  **|**  | //QualType// ``,`` //[QualType]// 
  | //QualType// | -> | //[QualTypeSegment]// 
  | //QualTypeSegment// | -> | //TypeIdent// 
  | //[QualTypeSegment]// | -> | //QualTypeSegment// 
  |  |  **|**  | //QualTypeSegment// ``.`` //[QualTypeSegment]// 
  | //[Decl]// | -> | **eps**
  |  |  **|**  | //Decl// //[Decl]// 
  | //Decl// | -> | ``type`` //TypeIdent// ``=`` //Type// ``;`` 
  |  |  **|**  | ``data`` //TypeIdent// ``=`` //[ConstrIdent]// ``;`` 
  |  |  **|**  | ``data`` //TypeIdent// ``<`` //[TypeIdent]// ``>`` ``=`` //[ConstrIdent]// ``;`` 
  |  |  **|**  | ``def`` //Type// //Ident// ``(`` //[Param]// ``)`` ``=`` //FunBody// ``;`` 
  |  |  **|**  | ``def`` //Type// //Ident// ``<`` //[TypeIdent]// ``>`` ``(`` //[Param]// ``)`` ``=`` //FunBody// ``;`` 
  |  |  **|**  | ``interface`` //TypeIdent// ``{`` //[MethSignat]// ``}`` 
  |  |  **|**  | ``interface`` //TypeIdent// ``extends`` //[QualType]// ``{`` //[MethSignat]// ``}`` 
  |  |  **|**  | ``class`` //TypeIdent// ``{`` //[ClassBody]// //MaybeBlock// //[ClassBody]// ``}`` 
  |  |  **|**  | ``class`` //TypeIdent// ``(`` //[Param]// ``)`` ``{`` //[ClassBody]// //MaybeBlock// //[ClassBody]// ``}`` 
  |  |  **|**  | ``class`` //TypeIdent// ``implements`` //[QualType]// ``{`` //[ClassBody]// //MaybeBlock// //[ClassBody]// ``}`` 
  |  |  **|**  | ``class`` //TypeIdent// ``(`` //[Param]// ``)`` ``implements`` //[QualType]// ``{`` //[ClassBody]// //MaybeBlock// //[ClassBody]// ``}`` 
  | //ConstrIdent// | -> | //TypeIdent// 
  |  |  **|**  | //TypeIdent// ``(`` //[ConstrType]// ``)`` 
  | //ConstrType// | -> | //Type// 
  |  |  **|**  | //Type// //Ident// 
  | //[ConstrType]// | -> | **eps**
  |  |  **|**  | //ConstrType// 
  |  |  **|**  | //ConstrType// ``,`` //[ConstrType]// 
  | //[TypeIdent]// | -> | //TypeIdent// 
  |  |  **|**  | //TypeIdent// ``,`` //[TypeIdent]// 
  | //[ConstrIdent]// | -> | //ConstrIdent// 
  |  |  **|**  | //ConstrIdent// ``|`` //[ConstrIdent]// 
  | //FunBody// | -> | ``builtin`` 
  |  |  **|**  | //PureExp// 
  | //MethSignat// | -> | //Type// //Ident// ``(`` //[Param]// ``)`` 
  | //[MethSignat]// | -> | **eps**
  |  |  **|**  | //MethSignat// ``;`` //[MethSignat]// 
  | //ClassBody// | -> | //Type// //Ident// ``;`` 
  |  |  **|**  | //Type// //Ident// ``=`` //PureExp// ``;`` 
  |  |  **|**  | //Type// //Ident// ``(`` //[Param]// ``)`` //Block// 
  | //[ClassBody]// | -> | **eps**
  |  |  **|**  | //ClassBody// //[ClassBody]// 
  | //Block// | -> | ``{`` //[Stm]// ``}`` 
  | //MaybeBlock// | -> | //Block// 
  |  |  **|**  | **eps**
  | //[Param]// | -> | **eps**
  |  |  **|**  | //Param// 
  |  |  **|**  | //Param// ``,`` //[Param]// 
  | //Param// | -> | //Type// //Ident// 
  | //[Stm]// | -> | **eps**
  |  |  **|**  | //Stm// //[Stm]// 
  | //Stm// | -> | //Exp// ``;`` 
  |  |  **|**  | ``{`` //[Stm]// ``}`` 
  |  |  **|**  | ``while`` ``(`` //PureExp// ``)`` //Stm// 
  |  |  **|**  | ``return`` //Exp// ``;`` 
  |  |  **|**  | //Ident// ``=`` //Exp// ``;`` 
  |  |  **|**  | ``this`` ``.`` //Ident// ``=`` //Exp// ``;`` 
  |  |  **|**  | //Type// //Ident// ``;`` 
  |  |  **|**  | //Type// //Ident// ``=`` //Exp// ``;`` 
  |  |  **|**  | ``if`` ``(`` //PureExp// ``)`` //Stm// 
  |  |  **|**  | ``if`` ``(`` //PureExp// ``)`` //Stm// ``else`` //Stm// 
  |  |  **|**  | ``suspend`` ``;`` 
  |  |  **|**  | ``skip`` ``;`` 
  |  |  **|**  | ``assert`` //PureExp// ``;`` 
  |  |  **|**  | ``await`` //Guard// ``;`` 
  | //Guard// | -> | //Ident// ``?`` 
  |  |  **|**  | ``this`` ``.`` //Ident// ``?`` 
  |  |  **|**  | //PureExp// 
  |  |  **|**  | //Guard// ``&`` //Guard// 
  | //Exp// | -> | //PureExp// 
  |  |  **|**  | //EffExp// 
  | //[PureExp]// | -> | **eps**
  |  |  **|**  | //PureExp// 
  |  |  **|**  | //PureExp// ``,`` //[PureExp]// 
  | //PureExp// | -> | //PureExp// ``||`` //PureExp1// 
  |  |  **|**  | //PureExp1// 
  |  |  **|**  | ``let`` ``(`` //Param// ``)`` ``=`` //PureExp// ``in`` //PureExp// 
  |  |  **|**  | ``if`` //PureExp// ``then`` //PureExp// ``else`` //PureExp// 
  |  |  **|**  | ``case`` //PureExp// ``{`` //[CaseBranch]// ``}`` 
  | //PureExp1// | -> | //PureExp1// ``&&`` //PureExp2// 
  |  |  **|**  | //PureExp2// 
  | //PureExp2// | -> | //PureExp2// ``==`` //PureExp3// 
  |  |  **|**  | //PureExp2// ``!=`` //PureExp3// 
  |  |  **|**  | //PureExp3// 
  | //PureExp3// | -> | //PureExp3// ``<`` //PureExp4// 
  |  |  **|**  | //PureExp3// ``<=`` //PureExp4// 
  |  |  **|**  | //PureExp3// ``>`` //PureExp4// 
  |  |  **|**  | //PureExp3// ``>=`` //PureExp4// 
  |  |  **|**  | //PureExp4// 
  | //PureExp4// | -> | //PureExp4// ``+`` //PureExp5// 
  |  |  **|**  | //PureExp4// ``-`` //PureExp5// 
  |  |  **|**  | //PureExp5// 
  | //PureExp5// | -> | //PureExp5// ``*`` //PureExp6// 
  |  |  **|**  | //PureExp5// ``/`` //PureExp6// 
  |  |  **|**  | //PureExp5// ``%`` //PureExp6// 
  |  |  **|**  | //PureExp6// 
  | //PureExp6// | -> | ``~`` //PureExp6// 
  |  |  **|**  | ``-`` //PureExp6// 
  |  |  **|**  | //PureExp7// 
  | //PureExp7// | -> | //Ident// ``(`` //[PureExp]// ``)`` 
  |  |  **|**  | //QualType// ``.`` //Ident// ``(`` //[PureExp]// ``)`` 
  |  |  **|**  | //Ident// ``[`` //[PureExp]// ``]`` 
  |  |  **|**  | //QualType// ``.`` //Ident// ``[`` //[PureExp]// ``]`` 
  |  |  **|**  | //Ident// 
  |  |  **|**  | ``this`` ``.`` //Ident// 
  |  |  **|**  | //QualType// ``.`` //Ident// 
  |  |  **|**  | //QualType// 
  |  |  **|**  | //QualType// ``(`` //[PureExp]// ``)`` 
  |  |  **|**  | //Literal// 
  |  |  **|**  | ``(`` //PureExp// ``)`` 
  | //CaseBranch// | -> | //Pattern// ``=>`` //PureExp// 
  | //[CaseBranch]// | -> | //CaseBranch// ``;`` 
  |  |  **|**  | //CaseBranch// ``;`` //[CaseBranch]// 
  | //[Pattern]// | -> | **eps**
  |  |  **|**  | //Pattern// 
  |  |  **|**  | //Pattern// ``,`` //[Pattern]// 
  | //Pattern// | -> | //Ident// 
  |  |  **|**  | //Literal// 
  |  |  **|**  | //TypeIdent// 
  |  |  **|**  | //TypeIdent// ``(`` //[Pattern]// ``)`` 
  |  |  **|**  | ``_`` 
  | //Literal// | -> | ``null`` 
  |  |  **|**  | ``this`` 
  |  |  **|**  | ``thisDC`` 
  |  |  **|**  | //String// 
  |  |  **|**  | //Integer// 
  | //EffExp// | -> | ``new`` //Type// ``(`` //[PureExp]// ``)`` 
  |  |  **|**  | ``new`` ``local`` //Type// ``(`` //[PureExp]// ``)`` 
  |  |  **|**  | //PureExp// ``.`` //Ident// ``(`` //[PureExp]// ``)`` 
  |  |  **|**  | ``this`` ``.`` //Ident// ``(`` //[PureExp]// ``)`` 
  |  |  **|**  | //PureExp// ``!`` //Ident// ``(`` //[PureExp]// ``)`` 
  |  |  **|**  | ``this`` ``!`` //Ident// ``(`` //[PureExp]// ``)`` 
  |  |  **|**  | //PureExp// ``.`` ``get`` 
  |  |  **|**  | //PureExp// ``spawns`` //Type// ``(`` //[PureExp]// ``)`` 


